-- Generated file. Read-only. Regenerate from rovno-db.
-- Do not edit this file directly.
-- Contract precedence: mirrored SQL is the exact audit source within backend-truth.
-- Source: supabase/migrations/20260727120000_drop_out_of_band_debug_rpcs.sql
-- Regenerate with: node scripts/generate-backend-truth.mjs
-- rovno-db audit #111: drop the three out-of-band __debug_% RPCs.
--
-- These were created by hand in Studio during the 2026-05-07 organizations-INSERT
-- debugging session and never removed. They appear in NO migration, which is why
-- every CI gate the repo has was blind to them for eleven weeks.
--
-- What they were, on Cloud staging, all three granted EXECUTE to anon AND
-- authenticated:
--
--   __debug_definer_insert_org()   SECURITY DEFINER, unconditional INSERT into
--                                  public.organizations. For any authenticated
--                                  caller this bypasses the organizations_insert
--                                  RLS policy outright: the caller lands as
--                                  owner_profile_id of a new organization. Its
--                                  slug uses epoch SECONDS, so the unique
--                                  constraint rate-limits this to about one row
--                                  per second rather than to one row. For anon it
--                                  fails only incidentally (auth.uid() is null and
--                                  owner_profile_id is NOT NULL), and the
--                                  function's own `exception when others` swallows
--                                  the 23502 and returns it as JSON. Nothing about
--                                  the design refuses an anonymous caller.
--   __debug_invoker_insert_org()   same INSERT without SECURITY DEFINER, so bounded
--                                  by RLS. Far less serious, still not something
--                                  that belongs in a schema.
--   __debug_jwt_state()            echoes current_setting('request.jwt.claims').
--                                  The caller's own JWT, so not a cross-user leak,
--                                  but an unauthenticated echo of auth internals.
--
-- Organizations are not inert: they feed the org-scoped RLS helpers
-- (20260506120100) and member_count (20260506120300:40), which feeds seat counting.
--
-- Checked before writing this: zero rows on staging match `name = 'definer probe'`
-- or `slug like 'definer-probe-%'`, so nobody ever called it and there is no data
-- to clean up. This migration only removes the functions.
--
-- PROD: these are in no migration, so the migration pipeline never carried them and
-- prod is not expected to have them. `drop function if exists` is a no-op there, so
-- this migration converges both environments by construction rather than leaving
-- prod status as an inference.
--
-- That convergence covers THESE THREE NAMES and nothing else. Prod is not otherwise
-- monitored for out-of-band objects: the nightly routine that runs
-- scripts/deployed-catalog-check.sql is staging-only by policy, and prod has its own
-- Studio, so the creation vector exists there too. Do not read this migration as
-- prod coverage.
--
-- This is the FOURTH time this class has had to be cleaned up:
--   20260507150000_drop_org_debug_rpcs      dropped __debug_org_rls_state()
--                                                   and __debug_try_insert_org(text)
--   20260513110000_drop_org_insert_probe    dropped the __org_insert_probe TABLE
--   20260726120000_drop_debug_org_rls_state2 dropped __debug_org_rls_state2()
--   this one                                drops the three Studio-created RPCs
--
-- The first three were all migration-borne, so supabase/tests/debug_rpcs_absent.test.sql
-- can catch a repeat. These three were not, and that suite runs against a
-- `db reset --no-seed` replay of supabase/migrations/**, so it is structurally
-- blind to anything created out of band. scripts/deployed-catalog-check.sql closes
-- that gap by asking the DEPLOYED catalog the same question; the nightly audit
-- routine runs it against staging every night.
--
-- Rule going forward: nothing in schema `public` may be named with a leading `__`.
-- If a diagnostic must exist, ship it through a migration, give it a name outside
-- that prefix, and restrict it to service_role.

drop function if exists public.__debug_definer_insert_org();
drop function if exists public.__debug_invoker_insert_org();
drop function if exists public.__debug_jwt_state();
