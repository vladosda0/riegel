-- Generated file. Read-only. Regenerate from rovno-db.
-- Do not edit this file directly.
-- Contract precedence: mirrored SQL is the exact audit source within backend-truth.
-- Source: supabase/migrations/20260729130000_project_invites_expires_at.sql
-- Regenerate with: node scripts/generate-backend-truth.mjs
-- Issue #101 — give a project invite an expiry, part 1 of 2: the column.
--
-- Split from the behaviour deliberately. `project_invites` is in the curated
-- backend-truth contract (rovno reads it as
-- WorkspaceDatabase["public"]["Tables"]["project_invites"]["Row"] in
-- src/data/workspace-source.ts:17 and src/lib/accept-project-invite.ts:20), so the
-- column MUST reach the generated types or the frontend can never read it. The
-- companion migration 20260729130100 carries the function, trigger and revoke,
-- whose statement families the mirror parser does not model, and is therefore
-- excluded from curation. Keeping them in one file would have forced the whole
-- thing out of the contract and silently dropped this column from the types.
--
-- 14 days is a product parameter, chosen rather than derived: long enough for a
-- contractor to act on an emailed invite without chasing, short enough that a
-- forgotten link does not stay live for months. It lives in the column default, so
-- changing it is a one-line migration.
--
-- Adding the column WITH the default backfills every existing row in the same
-- statement, which is deliberate: no invite that is live at deploy time dies on
-- deploy. Rows that are not `pending` get a value too and it is inert, because the
-- expiry check gates a FIRST acceptance only and that path is unreachable for them.
--
-- Nullable on purpose. A NULL means "never expires", the check in 20260729130100 is
-- null-safe, and leaving the column nullable avoids a NOT NULL constraint that any
-- future insert path could trip over for no safety gain.
--
-- No `comment on column` here, though the column deserves one: the mirror parser
-- does not model that statement either, and adding it would push this half out of
-- the contract too, which is the exact outcome the split exists to avoid. The
-- column's meaning is stated in this header and in 20260729130100's.
--
-- Idempotent: `add column if not exists`. Safe to re-apply, which matters because
-- prod applies with `supabase db push --include-all`.

alter table public.project_invites
  add column if not exists expires_at timestamptz default (now() + interval '14 days');
