-- Generated file. Read-only. Regenerate from rovno-db.
-- Do not edit this file directly.
-- Contract precedence: mirrored SQL is the exact audit source within backend-truth.
-- Source: supabase/migrations/20260726120000_drop_debug_org_rls_state2.sql
-- Regenerate with: node scripts/generate-backend-truth.mjs
-- rovno-db audit #95: drop public.__debug_org_rls_state2().
--
-- 20260507160000_org_policy_resilience.sql created it as SECURITY DEFINER and
-- granted EXECUTE to anon. An unauthenticated caller holding only the publishable
-- key could POST /rest/v1/rpc/__debug_org_rls_state2 and receive the verbatim
-- `qual` and `with_check` SQL of every policy on public.organizations, the
-- `with_check` of projects_insert, and whether RLS is enabled on the table at all.
--
-- No rows leak, which is why this is P1 and not P0. What leaks is a precise map of
-- the authorization model: the predicates name the helper functions they call
-- (is_org_member, can_manage_org), which tells a prober exactly which surface to
-- attack, and rls_enabled reports outright whether the table is currently
-- protected. SECURITY DEFINER means the caller needs no catalog access of their
-- own. A temporary debugging aid should not be a permanent unauthenticated
-- introspection endpoint.
--
-- This is the SECOND time this class has shipped VIA A MIGRATION.
-- 20260507150000_drop_org_debug_rpcs dropped the original __debug_org_rls_state(),
-- and this near-identical clone was introduced by a migration with a LATER
-- timestamp and never dropped, so the fix was silently undone.
-- supabase/tests/debug_rpcs_absent.test.sql now asserts the whole __debug_% class
-- rather than just this one name, so a third MIGRATION-BORNE reintroduction fails
-- CI.
--
-- That guarantee stops at the migration boundary, and the gap is real: the suite
-- runs against a `db reset` replay of supabase/migrations/**, so it cannot see a
-- function created by hand in Studio. Cloud staging carries three such functions
-- today (__debug_definer_insert_org, __debug_invoker_insert_org,
-- __debug_jwt_state), all granted to anon, the first of them a SECURITY DEFINER
-- that inserts into public.organizations. They are in no migration, this migration
-- does not remove them, and closing them is tracked separately.
--
-- Rule going forward: never grant a SECURITY DEFINER catalog-introspection RPC to
-- anon or authenticated. If a diagnostic must exist, restrict it to service_role.
--
-- Scope note: 20260507160000 also rebuilds the organizations policies. That work is
-- deliberately untouched here (its `organizations_update with check (true)` is a
-- separate open finding); this migration only removes the function.

drop function if exists public.__debug_org_rls_state2();
