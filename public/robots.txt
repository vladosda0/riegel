# A crawler obeys exactly one group - the most specific one matching its name -
# and ignores every other, so each named group below must repeat the shared
# Disallow rules. Omitting them from a named group grants that crawler access.
#
# /share/ and /invite/ carry the credential IN THE PATH: the share or invite
# token IS the URL. Only /share/document sets a noindex, and a meta tag is
# injected by JS, which a non-rendering crawler never runs - and every AI
# crawler named below is in that class. So the directive has to live here too.
#
# No group carries "Allow: /". Everything is allowed by default, so that line
# adds nothing, and placing it above a Disallow makes the file's meaning depend
# on the parser: under longest-match (RFC 9309 2.2.2, what Google/Bing/Yandex
# do) the Disallow still wins, but under first-match-in-order it does not, and
# python3 urllib.robotparser then reports /blog/admin as ALLOWED for every
# agent here. The AI crawlers' parsers are undocumented, so the file must not
# depend on which family they belong to.

User-agent: Googlebot
Disallow: /blog/admin
Disallow: /share/
Disallow: /invite/

User-agent: Bingbot
Disallow: /blog/admin
Disallow: /share/
Disallow: /invite/

User-agent: Twitterbot
Disallow: /blog/admin
Disallow: /share/
Disallow: /invite/

User-agent: facebookexternalhit
Disallow: /blog/admin
Disallow: /share/
Disallow: /invite/

User-agent: GPTBot
Disallow: /blog/admin
Disallow: /share/
Disallow: /invite/

User-agent: OAI-SearchBot
Disallow: /blog/admin
Disallow: /share/
Disallow: /invite/

User-agent: ChatGPT-User
Disallow: /blog/admin
Disallow: /share/
Disallow: /invite/

User-agent: ClaudeBot
Disallow: /blog/admin
Disallow: /share/
Disallow: /invite/

User-agent: PerplexityBot
Disallow: /blog/admin
Disallow: /share/
Disallow: /invite/

User-agent: YandexBot
Disallow: /blog/admin
Disallow: /share/
Disallow: /invite/

User-agent: *
Disallow: /blog/admin
Disallow: /share/
Disallow: /invite/

Sitemap: https://rovno.ai/sitemap.xml
